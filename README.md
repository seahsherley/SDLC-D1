# bundle

This branch is **generated output** — do not hand-edit it.

Contents are produced by `scripts/build-bundle.mjs` on the `main` branch of this
repository, which assembles a self-contained, deployable copy of the backend
(server + bundled frontend build + CLI). Re-run the build script to regenerate
this branch instead of editing files here directly.
